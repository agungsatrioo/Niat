<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.sera5.niat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.HistoryActivity"></activity>
        <activity android:name=".ui.activities.SuratActivity" />
        <activity android:name=".ui.activities.misc.SupportUsActivity" />
        <activity android:name=".ui.activities.misc.AboutActivity" />
        <activity android:name=".ui.activities.misc.AudioActivity" />
        <activity android:name=".ui.activities.misc.DasarHukumActivity" />
        <activity android:name=".ui.activities.misc.PengetahuanActivity" />
        <activity android:name=".ui.activities.AyatActivity" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>